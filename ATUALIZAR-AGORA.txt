HORIZONVILLE BACKEND — CORREÇÃO FINAL DE CORS

1) No GitHub do backend, substitua TODO o conteúdo pelos arquivos deste ZIP.
2) Confirme que package.json está na raiz e contém:
   "start": "node server.js"
3) No Render:
   Build Command: npm install
   Start Command: npm start
4) Faça:
   Manual Deploy -> Clear build cache & deploy
   (não apenas Deploy latest commit)
5) Quando ficar Live, abra:
   https://horizonville-backend.onrender.com/api/health

A resposta correta DESTA versão contém:
"corsVersion":"hv-cors-final-3"

Se não aparecer hv-cors-final-3, o Render ainda está executando outra versão/repositório/branch.

Nesta versão o backend envia:
Access-Control-Allow-Origin: *

A variável FRONTEND_ORIGIN pode continuar no Render, mas esta versão não depende dela para CORS.
