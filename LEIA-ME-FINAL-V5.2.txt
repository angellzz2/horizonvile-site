HORIZONVILLE BACKEND V5.2 FINAL

Deploy: Render (horizonville-backend).
Substitua os arquivos do backend pelo conteúdo deste ZIP e faça Manual Deploy / Deploy latest commit.

NÃO sobrescreva/apague seu data/store.json atual no repositório/volume caso ele contenha produtos e pedidos.
Este pacote propositalmente não contém store.json.

Variáveis necessárias no Render:
MTA_API_KEY = mesma chave configurada no resource horizon_idshop
HV_STORE_BOT_API_KEY = mesma chave do bot de logs
ADMIN_PASSWORD / ADMIN_TOKEN_SECRET = mantenha seus valores atuais
PAYMENT_MODE = demo apenas para testes

Testes após deploy:
/api/health -> build deve ser v5.2-final
/api/network-test -> ok true
/api/id-system/status -> mtaSeen deve ficar true com horizon_idshop iniciado

Compatibilidade MTA:
- aceita Authorization: Bearer MTA_API_KEY
- também aceita X-HV-MTA-KEY
- CORS robusto para horizonville.cc e Cloudflare Pages
